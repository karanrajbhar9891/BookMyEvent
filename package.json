{
  "name": "bookmyevent",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently --kill-others --names server,client --prefix name \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "start": "concurrently --kill-others --names server,client --prefix name \"npm --prefix server start\" \"npm --prefix client run preview\"",
    "install:all": "npm install --prefix server && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
